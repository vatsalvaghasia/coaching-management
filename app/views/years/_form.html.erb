<%= form_with(model: year) do |form| %>
  <% if year.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(year.errors.count, "error") %> prohibited this year from being saved:</h2>

      <ul>
        <% year.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.number_field :paper_year,class:'form-control',placeholder:'Year' %>
  </div>

  <div class="actions">
    <%= form.submit "Add Year",class:'btn btn-success' %>
  </div>
<% end %>
